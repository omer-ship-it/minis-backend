# Deployment

## CI/CD

GitHub Actions workflows are stored in `.github/workflows/`.

## Current Workflow

- `deploy.yml` builds and deploys the backend to Azure Web App.

## Requirements

- Repository secrets for Azure authentication
- Main branch push permissions
