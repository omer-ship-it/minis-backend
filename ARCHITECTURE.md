# Architecture

## Overview

This repository contains the backend API for minis.

## Components

- API layer (ASP.NET Core)
- Configuration via `appsettings*.json`
- Health endpoint under `/api/health`

## Notes

Move application code into `src/` and test projects into `tests/` as the codebase grows.
